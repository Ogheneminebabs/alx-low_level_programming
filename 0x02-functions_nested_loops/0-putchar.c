#include "main.h"

/**
 * main - my main description of the project
 *putchar - writes the character putchar to stdout
 * _putchar: the character to print
 *
 *
 *Return: on success 1
 * On error, -1 is returned, and errno is set appropriately
 */
char main(void)
{
char ch = _putchar;
putchar(ch);
return (0);
}
